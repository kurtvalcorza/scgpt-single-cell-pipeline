# Release status

Current status: **Candidate** — E2E adaptation tutorial source complete, awaiting supported clean-runtime execution. The pipeline package, 45 offline unit tests, one executed local CPU run of the committed notebook blob, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/scgpt_single_cell_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.

No licence or remote-code gate is open for DIMER hosting (MIT weights, plain safetensors into a torch re-implementation). Two things are open on the contract side: the fleet inventory's nominated first contract (multi-batch integration) is not implemented, and numerical parity of the re-implemented encoder with the upstream `scgpt` package has not been measured (bit-identical weights and functional lineage separation on real cells are the evidence). See `MODEL_CARD.md` (*DIMER deployment notes*).
