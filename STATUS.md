# Release status

Current status: **Candidate — clean-runtime qualified, promotion pending.** The pipeline package, 46 offline unit tests, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/scgpt_single_cell_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) pass static validation and GitHub Actions. Exact candidate commit `0f66318` / notebook blob `31e26691` also passed all 14 code cells in a fresh Kaggle Tesla T4 container with an empty cache and snapshot; the run exercised and digest-verified the immutable vocabulary fallback after Harvard Dataverse returned HTTP 504. Full evidence is recorded in `docs/release-verification.md`. The tutorial remains non-release-grade until the maintainer explicitly approves promotion.

No licence or remote-code gate is open for DIMER hosting (MIT weights, plain safetensors into a torch re-implementation). Two things are open on the contract side: the fleet inventory's nominated first contract (multi-batch integration) is not implemented, and numerical parity of the re-implemented encoder with the upstream `scgpt` package has not been measured (bit-identical weights and functional lineage separation on real cells are the evidence). See `MODEL_CARD.md` (*DIMER deployment notes*).
